import { ADD_USER } from "../actions/types";

const UserReducer = (state = {}, action) => {
    switch (action.type) {
        case ADD_USER:
            
            break;
    
        default:
            break;
    }
}

export default UserReducer;